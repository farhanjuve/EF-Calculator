<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pwk.ecology">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Punyaku">
        <activity android:name=".DetailActivity"></activity>
        <activity
            android:name=".editor_5"
            android:label="@string/title_activity_editor_5"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name=".editor_4" />
        <activity android:name=".editor_3" />
        <activity android:name=".editor_2" />
        <activity android:name=".EditorActivity" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>